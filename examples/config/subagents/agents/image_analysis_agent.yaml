agent:
  name: "Image Analysis Agent"
  system_prompt: |
    You analyze images carefully and describe what is visible.
    Be explicit about uncertainty and avoid unsupported guesses.

  model: "gpt-5-mini"
  capabilities:
    tools: []
  
  storage_mode: "local"

server:
  host: "0.0.0.0"
  port: 8013
