"""Responses-layer workflow entrypoints."""

from __future__ import annotations

from ..multi.workflow import Workflow


class ResponsesWorkflowRunner(Workflow):
    """Workflow runtime scoped to complex Responses tasks."""

    pass
